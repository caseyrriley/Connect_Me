<%= semantic_form_for(@connection) do |form| %>
<%= form.inputs do%>

<%= form.input :name %>
<%= form.input :phone_number %>
<%= form.input :email_address %>
<%= form.input :interested_in%>
<%= form.collection_select :user_id, User.all, :id, :ministry, :null => :false, :prompt => "Please select ministry to assign" %> 
<% end %>	
<br> 
<br>
<%= form.buttons %>
 
 <% end %>